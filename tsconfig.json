{
  "compilerOptions": {
    "baseUrl": "src",
    "lib": ["dom", "es2015"], // Set the base URL to the src directory
    "paths": {
      "@/*": ["*"] // Optional: Map '@/' to the src directory
    }
  }
}